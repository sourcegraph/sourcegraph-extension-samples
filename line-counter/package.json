{
  "name": "line-counter",
  "title": "WIP: Line Counter",
  "description": "Display the name and number of lines of code in a file.",
  "publisher": "sourcegraph",
  "activationEvents": [
    "*"
  ],
  "contributes": {
    "actions": [
      {
        "id": "linecounter.get",
        "command": "linecounter.displayLineCount",
        "actionItem": {
          "label": "Line Count",
          "description": "The line count for this file"
        }
      }
    ],
    "menus": {
      "editor/title": [
        {
          "action": "linecounter.get",
          "when": "resource"
        }
      ],
      "commandPalette": []
    },
    "configuration": {}
  },
  "version": "0.0.0-DEVELOPMENT",
  "license": "MIT",
  "main": "dist/sourcegraph-line-counter.js",
  "scripts": {
    "tslint": "tslint -p tsconfig.json './src/**/*.ts'",
    "typecheck": "tsc -p tsconfig.json",
    "build": "parcel build --out-file dist/sourcegraph-line-counter.js src/sourcegraph-line-counter.ts",
    "serve": "parcel serve --no-hmr --out-file dist/sourcegraph-line-counter.js src/sourcegraph-line-counter.ts",
    "watch:typecheck": "tsc -p tsconfig.json -w",
    "watch:build": "tsc -p tsconfig.dist.json -w",
    "sourcegraph:prepublish": "npm run build"
  },
  "browserslist": [
    "last 1 Chrome versions",
    "last 1 Firefox versions",
    "last 1 Edge versions",
    "last 1 Safari versions"
  ],
  "devDependencies": {
    "@sourcegraph/tsconfig": "^3.0.0",
    "@sourcegraph/tslint-config": "^12.1.0",
    "parcel-bundler": "^1.10.3",
    "sourcegraph": "^19.2.0",
    "tslint": "^5.11.0",
    "typescript": "^3.2.1"
  }
}
